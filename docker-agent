pipeline {
    agent {
        docker { image 'centos:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh '''
                sudo yum update -y 
                sudo yum install java-1.8.0-openjdk -y
                '''
            }
        }
    }
}
