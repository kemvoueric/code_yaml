pipeline {
    agent {
        docker { image 'centos:latest' }
    }
    stages {
        stage('Test') {
            steps {
                sh '''
                yum update -y 
                yum install java-1.8.0-openjdk -y
                '''
            }
        }
    }
}
